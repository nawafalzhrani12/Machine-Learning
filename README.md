# Machine Learning
Task 4 Answer: After running the feature selection step, the model performance remained very similar to the model trained with all features. This means that the removed features had little impact on predicting Order_Status. Feature selection was still helpful because it reduced the number of features and simplified the model without significantly affecting performance.
